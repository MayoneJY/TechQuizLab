<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mayonedev.battle.domain.battle.dao.BattleDao">

    <resultMap id="battleResultMap" type="com.mayonedev.battle.domain.battle.entity.Battle">
        <id property="userId" column="user_id"/>
        <id property="battleId" column="battle_id"/>
        <result property="stageId" column="stage_id"/>
        <result property="pfId" column="pf_id"/>
        <result property="totalDamage" column="total_damage"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="stageTitle" column="stage_title"/>
    </resultMap>

    <select id="findByUserAndBattleId" resultMap="battleResultMap">
        SELECT user_id, battle_id, stage_id, pf_id, total_damage, status, created_at
        FROM battle
        WHERE user_id = #{userId} AND battle_id = #{battleId}
    </select>

    <insert id="insert" parameterType="com.mayonedev.battle.domain.battle.entity.Battle">
        INSERT INTO battle (user_id, battle_id, stage_id, pf_id, total_damage, status, created_at)
        VALUES (#{userId}, #{battleId}, #{stageId}, #{pfId}, #{totalDamage}, #{status}, #{createdAt})
    </insert>

    <update id="updateStatus" parameterType="com.mayonedev.battle.domain.battle.entity.Battle">
        UPDATE battle
        SET status = #{status},
            total_damage = #{totalDamage}
        WHERE user_id = #{userId} AND battle_id = #{battleId}
    </update>

    <select id="findMaxBattleIdByUserId" resultType="java.lang.Long">
        SELECT MAX(battle_id) FROM battle WHERE user_id = #{userId}
    </select>

    <update id="updateTotalDamage" parameterType="com.mayonedev.battle.domain.battle.entity.Battle">
        UPDATE battle
        SET total_damage = #{totalDamage}
        WHERE user_id = #{userId} AND battle_id = #{battleId}
    </update>

    <select id="findAllByUserId" resultMap="battleResultMap">
        SELECT b.*, s.title as stage_title
        FROM battle b
        LEFT JOIN stage s ON b.stage_id = s.stage_id
        WHERE b.user_id = #{userId}
        ORDER BY b.created_at DESC
        LIMIT 5
    </select>

    <delete id="delete">
        DELETE FROM battle WHERE user_id = #{userId} AND battle_id = #{battleId}
    </delete>

    <select id="findBattleCategoriesByUserId" resultType="string">
        SELECT DISTINCT s.job_category
        FROM battle b
        JOIN stage s ON b.stage_id = s.stage_id
        WHERE b.user_id = #{userId}
        ORDER BY s.job_category
    </select>

    <update id="relinkPortfolio">
        UPDATE battle
        SET pf_id = #{newPfId}
        WHERE user_id = #{userId} AND pf_id = #{oldPfId}
    </update>

</mapper>
