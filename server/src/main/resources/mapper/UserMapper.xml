<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mayonedev.battle.dao.UserDao">

    <!-- Result Map -->
    <resultMap id="userResultMap" type="com.mayonedev.battle.entity.User">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- 모든 사용자 조회 -->
    <select id="findAll" resultMap="userResultMap">
        SELECT id, email, password, nickname, created_at
        FROM users
        ORDER BY created_at DESC
    </select>

    <!-- ID로 사용자 조회 -->
    <select id="findById" parameterType="long" resultMap="userResultMap">
        SELECT id, email, password, nickname, created_at
        FROM users
        WHERE id = #{id}
    </select>

    <!-- 이메일로 사용자 조회 (로그인용) -->
    <select id="findByEmail" parameterType="string" resultMap="userResultMap">
        SELECT id, email, password, nickname, created_at
        FROM users
        WHERE email = #{email}
    </select>

    <!-- 닉네임으로 사용자 조회 -->
    <select id="findByNickname" parameterType="string" resultMap="userResultMap">
        SELECT id, email, password, nickname, created_at
        FROM users
        WHERE nickname = #{nickname}
    </select>

    <!-- 사용자 등록 -->
    <insert id="insert" parameterType="com.mayonedev.battle.entity.User" 
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (email, password, nickname, created_at)
        VALUES (#{email}, #{password}, #{nickname}, #{createdAt})
    </insert>

    <!-- 사용자 정보 수정 -->
    <update id="update" parameterType="com.mayonedev.battle.entity.User">
        UPDATE users 
        SET email = #{email},
            password = #{password},
            nickname = #{nickname}
        WHERE id = #{id}
    </update>

    <!-- 사용자 삭제 -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM users 
        WHERE id = #{id}
    </delete>

    <!-- 이메일 중복 확인 -->
    <select id="existsByEmail" parameterType="string" resultType="boolean">
        SELECT COUNT(*) > 0 
        FROM users 
        WHERE email = #{email}
    </select>

    <!-- 닉네임 중복 확인 -->
    <select id="existsByNickname" parameterType="string" resultType="boolean">
        SELECT COUNT(*) > 0 
        FROM users 
        WHERE nickname = #{nickname}
    </select>

    <!-- 총 사용자 수 조회 -->
    <select id="count" resultType="int">
        SELECT COUNT(*) 
        FROM users
    </select>

</mapper>