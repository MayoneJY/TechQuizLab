<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mayonedev.battle.domain.user.dao.UserDao">

    <resultMap id="userResultMap" type="com.mayonedev.battle.domain.user.entity.User">
        <id property="userId" column="user_id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="level" column="level"/>
        <result property="exp" column="exp"/>
        <result property="createdAt" column="created_at"/>
        <result property="role" column="role"/>
        <result property="enabled" column="enabled"/>
        <result property="solvedCount" column="solved_count"/>
        <result property="currentStreak" column="current_streak"/>
        <result property="maxStreak" column="max_streak"/>
        <result property="lastLoginAt" column="last_login_at"/>
        <result property="remainingLives" column="remaining_lives"/>
        <result property="lastLivesResetAt" column="last_lives_reset_at"/>
    </resultMap>

    <select id="findAll" resultMap="userResultMap">
        SELECT user_id, email, password, nickname, level, exp, created_at, role, enabled,
               solved_count, current_streak, max_streak, last_login_at, remaining_lives, last_lives_reset_at
        FROM user
        ORDER BY created_at DESC
    </select>

    <select id="findById" parameterType="long" resultMap="userResultMap">
        SELECT user_id, email, password, nickname, level, exp, created_at, role, enabled,
               solved_count, current_streak, max_streak, last_login_at, remaining_lives, last_lives_reset_at
        FROM user
        WHERE user_id = #{id}
    </select>

    <select id="findByEmail" parameterType="string" resultMap="userResultMap">
        SELECT user_id, email, password, nickname, level, exp, created_at, role, enabled,
               solved_count, current_streak, max_streak, last_login_at, remaining_lives, last_lives_reset_at
        FROM user
        WHERE email = #{email}
    </select>

    <select id="findByNickname" parameterType="string" resultMap="userResultMap">
        SELECT user_id, email, password, nickname, level, exp, created_at, role, enabled,
               solved_count, current_streak, max_streak, last_login_at, remaining_lives, last_lives_reset_at
        FROM user
        WHERE nickname = #{nickname}
    </select>

    <insert id="insert" parameterType="com.mayonedev.battle.domain.user.entity.User" 
            useGeneratedKeys="true" keyProperty="userId">
        INSERT INTO user (email, password, nickname, level, exp, role, enabled, solved_count, current_streak, max_streak, remaining_lives, last_lives_reset_at)
        VALUES (#{email}, #{password}, #{nickname}, 1, 0, 'USER', 1, 0, 0, 0, 5, NOW())
    </insert>

    <update id="update" parameterType="com.mayonedev.battle.domain.user.entity.User">
        UPDATE user 
        SET email = #{email},
            password = #{password},
            nickname = #{nickname},
            level = #{level},
            exp = #{exp},
            role = #{role},
            enabled = #{enabled},
            solved_count = #{solvedCount},
            current_streak = #{currentStreak},
            max_streak = #{maxStreak},
            last_login_at = #{lastLoginAt},
            remaining_lives = #{remainingLives},
            last_lives_reset_at = #{lastLivesResetAt}
        WHERE user_id = #{userId}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM user 
        WHERE user_id = #{id}
    </delete>

    <select id="existsByEmail" parameterType="string" resultType="boolean">
        SELECT COUNT(*) > 0 
        FROM user 
        WHERE email = #{email}
    </select>

    <select id="existsByNickname" parameterType="string" resultType="boolean">
        SELECT COUNT(*) > 0 
        FROM user 
        WHERE nickname = #{nickname}
    </select>

    <select id="count" resultType="int">
        SELECT COUNT(*) 
        FROM user
    </select>

    <!-- 랭킹 조회 (Top 10) -->
    <select id="findRankings" resultMap="userResultMap">
        SELECT user_id, email, nickname, level, exp, created_at, role, enabled,
               solved_count, current_streak, max_streak, last_login_at
        FROM user
        ORDER BY exp DESC, level DESC, user_id ASC
        LIMIT 10
    </select>

</mapper>
