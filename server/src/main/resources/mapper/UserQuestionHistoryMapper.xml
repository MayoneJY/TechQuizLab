<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mayonedev.battle.dao.UserQuestionHistoryDao">

    <resultMap id="historyResultMap" type="com.mayonedev.battle.entity.UserQuestionHistory">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="questionId" column="question_id"/>
        <result property="isCorrect" column="is_correct"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="findByUserId" parameterType="long" resultMap="historyResultMap">
        SELECT id, user_id, question_id, is_correct, created_at
        FROM user_question_history
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="com.mayonedev.battle.entity.UserQuestionHistory" 
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_question_history (user_id, question_id, is_correct, created_at)
        VALUES (#{userId}, #{questionId}, #{isCorrect}, #{createdAt})
    </insert>

    <select id="countByUserIdAndCorrect" resultType="int">
        SELECT COUNT(*)
        FROM user_question_history
        WHERE user_id = #{userId} AND is_correct = #{isCorrect}
    </select>

</mapper>
