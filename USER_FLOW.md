# 잡스페이스 (Job Space) - 유저 플로우 문서

## 📋 목차
1. [전체 유저 플로우](#전체-유저-플로우)
2. [화면별 상세 플로우](#화면별-상세-플로우)
3. [기능별 플로우](#기능별-플로우)
4. [API 연동 플로우](#api-연동-플로우)
5. [에러 처리 플로우](#에러-처리-플로우)

---

## 전체 유저 플로우

```
시작
  ↓
[홈 화면]
  ├─→ [로그인/회원가입] → [홈 화면]
  ├─→ [주제 선택] → [게임 시작]
  ├─→ [업적 확인]
  ├─→ [일일 미션 확인]
  └─→ [게임 정보 (ABOUT)]
  ↓
[게임 화면]
  ├─→ [문제 풀이] → [정답/오답 피드백]
  ├─→ [오답 노트 추가]
  └─→ [다음 문제]
  ↓
[게임 종료]
  ├─→ [게임 오버] → [재시작] / [홈으로]
  └─→ [승리] → [재시작] / [홈으로]
```

---

## 화면별 상세 플로우

### 1. 홈 화면 (Home.vue)

#### 진입 플로우
```
홈 화면 로드
  ↓
[초기화]
  ├─→ 주제 목록 API 호출 (GET /api/topics)
  ├─→ 로그인 상태 확인
  │   ├─→ 로그인됨: 업적/미션 정보 로드
  │   └─→ 미로그인: 기본 화면 표시
  └─→ 첫 번째 주제 자동 선택
```

#### 주요 액션
- **START 버튼 클릭**
  ```
  START 클릭
    ↓
  로그인 확인
    ├─→ 미로그인 → 로그인 페이지로 이동
    └─→ 로그인됨 → 주제 선택 확인
         ├─→ 주제 미선택 → 알림 표시
         └─→ 주제 선택됨 → 문제 로드 → 게임 화면 이동
  ```

- **OPTION 버튼 클릭**
  ```
  OPTION 클릭
    ↓
  주제 선택 화면 표시/숨김 토글
    ↓
  주제 선택
    ↓
  선택된 주제 하이라이트
  ```

- **ABOUT 버튼 클릭**
  ```
  ABOUT 클릭
    ↓
  게임 설명 화면 표시/숨김 토글
  ```

- **업적/미션 확인**
  ```
  업적/미션 버튼 클릭
    ↓
  API 호출
    ├─→ GET /api/gamification/achievements
    ├─→ GET /api/gamification/my-achievements?userId={id}
    └─→ GET /api/gamification/missions/daily?userId={id}
    ↓
  업적/미션 목록 표시
  ```

---

### 2. 로그인 화면 (Login.vue)

#### 로그인 플로우
```
로그인 화면 진입
  ↓
이메일/비밀번호 입력
  ↓
로그인 버튼 클릭
  ↓
유효성 검사
  ├─→ 실패 → 에러 메시지 표시
  └─→ 성공 → API 호출
       ↓
      POST /api/users/login
       ↓
      응답 처리
       ├─→ 성공: 토큰 저장 → 홈 화면 이동
       └─→ 실패: 에러 메시지 표시
```

#### 회원가입 플로우
```
회원가입 탭 선택
  ↓
이메일/닉네임/비밀번호 입력
  ↓
이메일 중복 확인 (선택)
  ↓
닉네임 중복 확인 (선택)
  ↓
회원가입 버튼 클릭
  ↓
유효성 검사
  ├─→ 비밀번호 불일치 → 에러 표시
  └─→ 성공 → API 호출
       ↓
      POST /api/users
       ↓
      응답 처리
       ├─→ 성공: 자동 로그인 → 홈 화면 이동
       └─→ 실패: 에러 메시지 표시
```

---

### 3. 게임 화면 (Game.vue)

#### 게임 시작 플로우
```
게임 화면 진입
  ↓
문제 로드 확인
  ├─→ 문제 없음 → 홈으로 리다이렉트
  └─→ 문제 있음 → 첫 번째 문제 표시
       ↓
      게임 상태 초기화
      - 점수: 0
      - 생명력: 8
      - 현재 문제 인덱스: 0
```

#### 문제 풀이 플로우
```
문제 표시
  ↓
답안 입력
  ↓
제출 버튼 클릭 / Enter 키
  ↓
답안 제출 API 호출
  ↓
POST /api/questions/{id}/submit
  ↓
응답 처리
  ├─→ 정답
  │   ├─→ 점수 +100
  │   ├─→ 정답 피드백 표시
  │   ├─→ 폭발 효과 (녹색)
  │   └─→ 2초 후 다음 문제
  │
  └─→ 오답
      ├─→ 생명력 -1
      ├─→ 오답 피드백 표시
      ├─→ 정답 표시
      ├─→ 폭발 효과 (빨간색)
      ├─→ 북마크 버튼 표시
      └─→ 2초 후 다음 문제
```

#### 북마크 플로우
```
오답 문제에서 북마크 버튼 클릭
  ↓
POST /api/questions/{id}/bookmark
  ↓
응답 처리
  ├─→ 성공: 알림 표시
  └─→ 실패: 에러 메시지 표시
```

#### 게임 종료 플로우
```
모든 문제 완료
  ↓
승리 화면 표시
  ├─→ 최종 점수 표시
  ├─→ 재시작 버튼
  └─→ 홈으로 버튼

또는

생명력 0 도달
  ↓
게임 오버 화면 표시
  ├─→ 최종 점수 표시
  ├─→ 몬스터 애니메이션
  ├─→ 재시작 버튼
  └─→ 홈으로 버튼
```

---

## 기능별 플로우

### 1. 인증 플로우

```
[인증 필요 기능 접근]
  ↓
로그인 상태 확인
  ├─→ 로그인됨 → 기능 실행
  └─→ 미로그인 → 로그인 페이지로 이동
       ↓
      로그인 완료
       ↓
      원래 기능으로 복귀
```

### 2. 주제 선택 플로우

```
홈 화면
  ↓
OPTION 버튼 클릭
  ↓
주제 목록 표시
  ↓
주제 선택
  ↓
선택된 주제 하이라이트
  ↓
START 버튼 활성화
```

### 3. 업적 시스템 플로우

```
홈 화면
  ↓
업적 버튼 클릭
  ↓
API 호출
  ├─→ 전체 업적 목록 (GET /api/gamification/achievements)
  └─→ 내 업적 목록 (GET /api/gamification/my-achievements?userId={id})
  ↓
업적 목록 표시
  ├─→ 달성한 업적: 금색 하이라이트
  └─→ 미달성 업적: 회색 표시
```

### 4. 일일 미션 플로우

```
홈 화면
  ↓
일일 미션 버튼 클릭
  ↓
API 호출 (GET /api/gamification/missions/daily?userId={id})
  ↓
미션 목록 표시
  ├─→ 진행률 표시
  ├─→ 완료된 미션: 보상 받기 버튼
  └─→ 미완료 미션: 진행률 표시
  ↓
보상 받기 클릭
  ↓
POST /api/gamification/missions/{id}/claim
  ↓
보상 수령 완료
```

---

## API 연동 플로우

### 1. 문제 로드 플로우

```
게임 시작
  ↓
주제 ID 확인
  ↓
GET /api/questions?topicId={id}
  ↓
응답 처리
  ├─→ 성공: 문제 목록 저장 → 첫 문제 표시
  └─→ 실패: 에러 메시지 → 홈으로 이동
```

### 2. 답안 제출 플로우

```
답안 입력
  ↓
제출 버튼 클릭
  ↓
POST /api/questions/{id}/submit
  Body: { userId, answer }
  ↓
응답 처리
  ├─→ { isCorrect: true } → 정답 처리
  └─→ { isCorrect: false } → 오답 처리
```

### 3. 북마크 플로우

```
북마크 버튼 클릭
  ↓
POST /api/questions/{id}/bookmark
  Body: { userId, memo? }
  ↓
응답 처리
  ├─→ 성공: 알림 표시
  └─→ 실패: 에러 메시지 표시
```

### 4. 인증 플로우

```
로그인 요청
  ↓
POST /api/users/login
  Body: { email, password }
  ↓
응답 처리
  ├─→ 성공: 토큰 저장 → 사용자 정보 저장
  └─→ 실패: 에러 메시지 표시
  ↓
모든 API 요청에 토큰 포함
  Authorization: Bearer {token}
```

---

## 에러 처리 플로우

### 1. 네트워크 에러

```
API 호출 실패
  ↓
에러 타입 확인
  ├─→ 네트워크 에러 → "서버에 연결할 수 없습니다" 메시지
  └─→ 타임아웃 → "요청 시간이 초과되었습니다" 메시지
  ↓
사용자에게 알림 표시
```

### 2. 인증 에러

```
401 Unauthorized 응답
  ↓
토큰 만료/무효 처리
  ├─→ 로컬 스토리지 토큰 삭제
  ├─→ 사용자 정보 삭제
  └─→ 로그인 페이지로 리다이렉트
```

### 3. 유효성 검사 에러

```
입력값 검증 실패
  ├─→ 이메일 형식 오류
  ├─→ 비밀번호 불일치
  ├─→ 필수 필드 누락
  └─→ 중복 확인 실패
  ↓
에러 메시지 표시
  ↓
사용자가 수정 가능
```

### 4. 게임 에러

```
게임 진행 중 에러
  ├─→ 문제 로드 실패 → 홈으로 이동
  ├─→ 답안 제출 실패 → 재시도 가능
  └─→ 북마크 실패 → 알림만 표시
```

---

## 상태 관리 플로우

### Pinia Store 구조

```
authStore (인증)
  ├─→ user: 사용자 정보
  ├─→ token: 인증 토큰
  ├─→ isAuthenticated: 로그인 상태
  └─→ login(), register(), logout()

topicStore (주제)
  ├─→ topics: 주제 목록
  └─→ fetchTopics()

questionStore (문제)
  ├─→ questions: 문제 목록
  ├─→ currentQuestion: 현재 문제
  ├─→ bookmarks: 북마크 목록
  └─→ fetchQuestionsByTopic(), submitAnswer(), bookmarkQuestion()

gameStore (게임)
  ├─→ score: 점수
  ├─→ lives: 생명력
  ├─→ currentQuestionIndex: 현재 문제 인덱스
  ├─→ gameStatus: 게임 상태 (playing/victory/gameOver)
  └─→ submitAnswer(), nextQuestion(), resetGame()

gamificationStore (게이미피케이션)
  ├─→ achievements: 전체 업적
  ├─→ userAchievements: 내 업적
  ├─→ dailyMissions: 일일 미션
  └─→ fetchAllAchievements(), fetchUserAchievements(), fetchDailyMissions()
```

---

## 화면 전환 플로우

```
/ (홈)
  ├─→ /login (로그인)
  │   └─→ / (홈) [로그인 성공]
  │
  ├─→ /game (게임)
  │   ├─→ / (홈) [게임 종료]
  │   └─→ /game (재시작)
  │
  └─→ / (홈) [로그아웃]
```

---

## 주요 사용자 시나리오

### 시나리오 1: 신규 사용자 게임 플레이

```
1. 홈 화면 진입
2. 회원가입 클릭
3. 이메일/닉네임/비밀번호 입력
4. 회원가입 완료 → 자동 로그인
5. 주제 선택
6. START 버튼 클릭
7. 문제 풀이 시작
8. 답안 입력 및 제출
9. 정답/오답 피드백 확인
10. 모든 문제 완료 또는 생명력 소진
11. 게임 결과 확인
12. 재시작 또는 홈으로 이동
```

### 시나리오 2: 기존 사용자 업적 확인

```
1. 홈 화면 진입
2. 로그인 (이미 로그인됨)
3. 업적 버튼 클릭
4. 업적 목록 확인
5. 일일 미션 버튼 클릭
6. 미션 진행률 확인
7. 완료된 미션 보상 수령
8. 게임 시작
```

### 시나리오 3: 오답 노트 활용

```
1. 게임 진행 중 오답 발생
2. 오답 피드백 확인
3. 북마크 버튼 클릭
4. 오답 노트에 추가 완료
5. 게임 계속 진행
6. 나중에 오답 노트에서 복습 (향후 기능)
```

---

## 기술 스택 및 아키텍처

### 프론트엔드
- **Framework**: Vue 3 (Composition API)
- **State Management**: Pinia
- **Routing**: Vue Router
- **HTTP Client**: Axios
- **Styling**: CSS3 (Pixel Art Theme)

### 백엔드 API
- **Base URL**: `https://battle.mayonedev.com`
- **Authentication**: Bearer Token
- **API Documentation**: Swagger UI

### 주요 컴포넌트
- `CustomCursor.vue`: 우주선 커서
- `CursorTrail.vue`: 커서 궤적 효과
- `ParticleBackground.vue`: 파티클 배경
- `ExplosionEffect.vue`: 폭발 효과
- `PixelHeart.vue`: 하트 아이콘
- `PixelSpaceship.vue`: 우주선 아이콘
- `PixelMonster.vue`: 몬스터 아이콘

---

## 향후 개선 사항

1. **오답 노트 화면**: 북마크한 문제들을 모아서 볼 수 있는 화면
2. **배틀 모드**: 다른 사용자와 실시간 배틀
3. **레벨 시스템**: 주제별 레벨 및 경험치 시스템
4. **리더보드**: 점수 랭킹 시스템
5. **통계 화면**: 학습 통계 및 분석

---

## 버전 정보

- **Version**: 1.0.0
- **Last Updated**: 2024
- **Author**: 잡스페이스 개발팀

